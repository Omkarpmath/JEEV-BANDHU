<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sales Dashboard - JeevBandhu</title>
    <link rel="stylesheet" href="/css/output.css">
</head>

<body class="bg-gradient-to-br from-emerald-50 via-white to-blue-50 min-h-screen">
    <!-- Navigation -->
    <nav class="bg-white shadow-md">
        <div class="max-w-full xl:max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
                <a href="/dashboard" class="text-2xl font-bold text-emerald-600">🐄 JeevBandhu</a>

                <!-- Desktop Navigation -->
                <div class="hidden md:flex items-center space-x-6">
                    <a href="/dashboard" class="text-gray-700 hover:text-emerald-600 transition">Dashboard</a>
                    <a href="/my-products" class="text-gray-700 hover:text-emerald-600 transition">My Products</a>
                    <a href="/orders/sales" class="text-emerald-600 font-semibold">Sales</a>
                    <span class="text-gray-600">Hi, <%= user.name %></span>
                    <form action="/logout" method="POST" class="inline">
                        <button type="submit" class="text-red-600 hover:text-red-700 transition">Logout</button>
                    </form>
                </div>

                <!-- Mobile Hamburger Button -->
                <button id="salesMenuBtn"
                    class="md:hidden flex flex-col justify-center items-center w-10 h-10 hamburger z-50 relative">
                    <span class="block w-6 h-0.5 bg-gray-700 mb-1"></span>
                    <span class="block w-6 h-0.5 bg-gray-700 mb-1"></span>
                    <span class="block w-6 h-0.5 bg-gray-700"></span>
                </button>
            </div>
        </div>

        <!-- Mobile Menu Overlay -->
        <div id="salesMenu"
            class="mobile-menu hidden fixed top-0 right-0 h-full w-64 bg-white shadow-2xl z-40 md:hidden overflow-y-auto">
            <div class="flex flex-col p-6 pt-20 space-y-4">
                <a href="/dashboard"
                    class="text-gray-700 hover:text-emerald-600 font-medium py-2 border-b border-gray-200">🏠
                    Dashboard</a>
                <a href="/my-products"
                    class="text-gray-700 hover:text-emerald-600 font-medium py-2 border-b border-gray-200">📦 My
                    Products</a>
                <a href="/orders/sales" class="text-emerald-600 font-semibold py-2 border-b border-gray-200">💰
                    Sales</a>
                <a href="/marketplace"
                    class="text-gray-700 hover:text-emerald-600 font-medium py-2 border-b border-gray-200">🛒
                    Marketplace</a>
                <span class="text-gray-600 px-3 py-2 bg-emerald-50 rounded-full text-center text-sm">Hi, <%= user.name
                        %></span>
                <form action="/logout" method="POST" class="inline">
                    <button type="submit"
                        class="w-full text-left text-red-600 hover:text-red-700 font-medium py-2">Logout</button>
                </form>
            </div>
        </div>
    </nav>

    <main class="max-w-full xl:max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <div class="mb-8">
            <h1 class="text-3xl font-bold text-gray-900">📊 Sales Dashboard</h1>
            <p class="text-gray-600 mt-2">Track your orders and revenue</p>
        </div>

        <!-- Statistics Cards -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
            <div
                class="bg-white rounded-xl shadow-lg border-2 border-emerald-200 p-6 hover:shadow-xl transition-shadow">
                <div class="flex items-center justify-between">
                    <div>
                        <p class="text-sm font-semibold text-gray-600 mb-1">💰 Total Revenue</p>
                        <p class="text-3xl font-bold text-emerald-600">₹<%= stats.totalRevenue.toFixed(2) %>
                        </p>
                    </div>
                    <div class="w-16 h-16 bg-emerald-100 rounded-full flex items-center justify-center">
                        <span class="text-3xl">💰</span>
                    </div>
                </div>
            </div>
            <div class="bg-white rounded-xl shadow-lg border-2 border-blue-200 p-6 hover:shadow-xl transition-shadow">
                <div class="flex items-center justify-between">
                    <div>
                        <p class="text-sm font-semibold text-gray-600 mb-1">📦 Total Orders</p>
                        <p class="text-3xl font-bold text-blue-600">
                            <%= stats.totalOrders %>
                        </p>
                    </div>
                    <div class="w-16 h-16 bg-blue-100 rounded-full flex items-center justify-center">
                        <span class="text-3xl">📦</span>
                    </div>
                </div>
            </div>
            <div class="bg-white rounded-xl shadow-lg border-2 border-yellow-200 p-6 hover:shadow-xl transition-shadow">
                <div class="flex items-center justify-between">
                    <div>
                        <p class="text-sm font-semibold text-gray-600 mb-1">⏳ Pending Orders</p>
                        <p class="text-3xl font-bold text-yellow-600">
                            <%= stats.pendingOrders %>
                        </p>
                    </div>
                    <div class="w-16 h-16 bg-yellow-100 rounded-full flex items-center justify-center">
                        <span class="text-3xl">⏳</span>
                    </div>
                </div>
            </div>
        </div>

        <!-- Orders List -->
        <div class="bg-white rounded-lg shadow-md border border-gray-200 overflow-hidden">
            <div class="px-6 py-6 border-b border-gray-200 bg-gradient-to-r from-emerald-50 to-blue-50">
                <h2 class="text-2xl font-bold text-gray-900">📋 Recent Orders</h2>
            </div>

            <% if (orders.length===0) { %>
                <div class="text-center py-16">
                    <div class="text-6xl mb-4">📦</div>
                    <h3 class="text-xl font-bold text-gray-700 mb-2">No Sales Yet</h3>
                    <p class="text-gray-600">List products to start receiving orders!</p>
                </div>
                <% } else { %>
                    <!-- Desktop Table View (hidden on mobile) -->
                    <div class="hidden md:block overflow-x-auto p-6">
                        <table class="min-w-full table-auto">
                            <thead class="bg-gray-50 border-b-2 border-gray-200">
                                <tr>
                                    <th
                                        class="px-4 py-3 text-left text-xs font-bold text-gray-700 uppercase tracking-wider">
                                        Order ID</th>
                                    <th
                                        class="px-4 py-3 text-left text-xs font-bold text-gray-700 uppercase tracking-wider">
                                        Product</th>
                                    <th
                                        class="px-4 py-3 text-left text-xs font-bold text-gray-700 uppercase tracking-wider">
                                        Quantity</th>
                                    <th
                                        class="px-4 py-3 text-left text-xs font-bold text-gray-700 uppercase tracking-wider">
                                        Amount</th>
                                    <th
                                        class="px-4 py-3 text-left text-xs font-bold text-gray-700 uppercase tracking-wider">
                                        Status</th>
                                    <th
                                        class="px-4 py-3 text-left text-xs font-bold text-gray-700 uppercase tracking-wider">
                                        Date</th>
                                    <th
                                        class="px-4 py-3 text-left text-xs font-bold text-gray-700 uppercase tracking-wider">
                                        Actions</th>
                                </tr>
                            </thead>
                            <tbody class="bg-white">
                                <% orders.forEach((order, index)=> { %>
                                    <tr
                                        class="border-b border-gray-100 hover:bg-emerald-50 transition-colors <%= index % 2 === 0 ? 'bg-white' : 'bg-gray-50' %>">
                                        <td class="px-4 py-4 whitespace-nowrap">
                                            <span class="text-sm font-bold text-gray-900">#<%=
                                                    order._id.toString().substring(0, 8).toUpperCase() %></span>
                                        </td>
                                        <td class="px-4 py-4 whitespace-nowrap">
                                            <div class="text-sm font-semibold text-gray-900 mb-1">
                                                <%= order.productType %>
                                            </div>
                                            <div class="text-xs text-gray-500">🏷️ <%= order.animalTagId %>
                                            </div>
                                        </td>
                                        <td class="px-4 py-4 whitespace-nowrap">
                                            <span class="text-sm text-gray-900 font-medium">
                                                <%= order.quantity %>
                                                    <%= order.unit %>
                                            </span>
                                        </td>
                                        <td class="px-4 py-4 whitespace-nowrap">
                                            <span class="text-lg font-bold text-emerald-600">₹<%=
                                                    order.totalPrice.toFixed(2) %></span>
                                        </td>
                                        <td class="px-4 py-4 whitespace-nowrap">
                                            <% if (order.status==='pending' ) { %>
                                                <span
                                                    class="inline-flex items-center px-3 py-1.5 rounded-full text-xs font-semibold bg-yellow-100 text-yellow-800">⏳
                                                    Pending</span>
                                                <% } else if (order.status==='confirmed' ) { %>
                                                    <span
                                                        class="inline-flex items-center px-3 py-1.5 rounded-full text-xs font-semibold bg-blue-100 text-blue-800">✅
                                                        Confirmed</span>
                                                    <% } else if (order.status==='delivered' ) { %>
                                                        <span
                                                            class="inline-flex items-center px-3 py-1.5 rounded-full text-xs font-semibold bg-green-100 text-green-800">🎉
                                                            Delivered</span>
                                                        <% } else if (order.status==='cancelled' ) { %>
                                                            <span
                                                                class="inline-flex items-center px-3 py-1.5 rounded-full text-xs font-semibold bg-red-100 text-red-800">❌
                                                                Cancelled</span>
                                                            <% } %>
                                        </td>
                                        <td class="px-4 py-4 whitespace-nowrap">
                                            <span class="text-sm text-gray-600">
                                                <%= new Date(order.createdAt).toLocaleDateString('en-IN') %>
                                            </span>
                                        </td>
                                        <td class="px-4 py-4 whitespace-nowrap">
                                            <% if (order.status==='pending' ) { %>
                                                <div class="flex flex-col space-y-2">
                                                    <button onclick="updateStatus('<%= order._id %>', 'confirmed')"
                                                        class="px-4 py-2 bg-emerald-600 text-white rounded-lg hover:bg-emerald-700 font-medium text-xs transition shadow-sm">✅
                                                        Confirm</button>
                                                    <button onclick="updateStatus('<%= order._id %>', 'cancelled')"
                                                        class="px-4 py-2 bg-red-600 text-white rounded-lg hover:bg-red-700 font-medium text-xs transition shadow-sm">❌
                                                        Cancel</button>
                                                </div>
                                                </h4>
                                                <p class="text-sm text-gray-700">
                                                    <span class="font-medium">Method:</span>
                                                    <%= order.paymentMethod || 'Cash on Delivery' %>
                                                </p>
                                                <p class="text-sm text-gray-700 mt-1">
                                                    <span class="font-medium">Status:</span>
                                                    <% if (order.paymentStatus==='completed' ) { %>
                                                        <span class="text-green-600 font-semibold">✅
                                                            Completed</span>
                                                        <% } else if (order.paymentStatus==='failed' ) { %>
                                                            <span class="text-red-600 font-semibold">❌
                                                                Failed</span>
                                                            <% } else { %>
                                                                <span class="text-yellow-600 font-semibold">⏳
                                                                    Pending</span>
                                                                <% } %>
                                                </p>
                    </div>
        </div>
        </td>
        </tr>
        <% } else if (order.status==='confirmed' ) { %>
            <div class="mt-3 pt-3 border-t border-gray-200">
                <button onclick="updateStatus('<%= order._id %>', 'delivered')"
                    class="w-full px-4 py-2 bg-emerald-600 text-white rounded-lg hover:bg-emerald-700 font-medium text-sm transition shadow-sm">🎉
                    Mark Delivered</button>
            </div>
            <% } %>
                </td>
                </tr>
                <% }) %>
                    </tbody>
                    </table>
                    </div>


                    <!-- Mobile Card View (hidden on desktop) -->
                    <div class="md:hidden p-4 space-y-4">
                        <%orders.forEach((order, index)=> { %>
                            <div
                                class="bg-white border-2 border-gray-200 rounded-lg p-4 hover:shadow-lg transition-shadow">
                                <div class="flex justify-between items-start mb-3 pb-3 border-b border-gray-200">
                                    <div>
                                        <div class="text-xs text-gray-500 mb-1">Order ID</div>
                                        <div class="text-sm font-bold text-gray-900">#
                                            <%=order._id.toString().substring(0, 8).toUpperCase() %>
                                        </div>
                                    </div>
                                    <div>
                                        <% if (order.status==='pending' ) { %>
                                            <span
                                                class="inline-flex items-center px-3 py-1.5 rounded-full text-xs font-semibold bg-yellow-100 text-yellow-800">⏳
                                                Pending</span>
                                            <% } else if (order.status==='confirmed' ) { %>
                                                <span
                                                    class="inline-flex items-center px-3 py-1.5 rounded-full text-xs font-semibold bg-blue-100 text-blue-800">✅
                                                    Confirmed</span>
                                                <% } else if (order.status==='delivered' ) { %>
                                                    <span
                                                        class="inline-flex items-center px-3 py-1.5 rounded-full text-xs font-semibold bg-green-100 text-green-800">🎉
                                                        Delivered</span>
                                                    <% } else if (order.status==='cancelled' ) { %>
                                                        <span
                                                            class="inline-flex items-center px-3 py-1.5 rounded-full text-xs font-semibold bg-red-100 text-red-800">❌
                                                            Cancelled</span>
                                                        <% } %>
                                    </div>
                                </div>
                                <div class="space-y-2 mb-3">
                                    <div class="flex justify-between"><span
                                            class="text-sm text-gray-600">Product:</span><span
                                            class="text-sm font-semibold text-gray-900">
                                            <%=order.productType %>
                                        </span></div>
                                    <div class="flex justify-between"><span
                                            class="text-sm text-gray-600">Source:</span><span
                                            class="text-sm text-gray-700">🏷️ <%=order.animalTagId %></span></div>
                                    <div class="flex justify-between"><span
                                            class="text-sm text-gray-600">Quantity:</span><span
                                            class="text-sm font-medium text-gray-900">
                                            <%=order.quantity %>
                                                <%=order.unit %>
                                        </span></div>
                                    <div class="flex justify-between"><span
                                            class="text-sm text-gray-600">Amount:</span><span
                                            class="text-lg font-bold text-emerald-600">₹<%=order.totalPrice.toFixed(2)
                                                %></span></div>
                                    <div class="flex justify-between"><span
                                            class="text-sm text-gray-600">Date:</span><span
                                            class="text-sm text-gray-700">
                                            <%=new Date(order.createdAt).toLocaleDateString('en-IN') %>
                                        </span></div>
                                </div>
                                <% if (order.status==='pending' ) { %>
                                    <div class="grid grid-cols-2 gap-2 mt-3 pt-3 border-t border-gray-200">
                                        <button onclick="updateStatus('<%=order._id %>', 'confirmed')"
                                            class="px-4 py-2 bg-emerald-600 text-white rounded-lg hover:bg-emerald-700 font-medium text-sm transition shadow-sm">✅
                                            Confirm</button>
                                        <button onclick="updateStatus('<%=order._id %>', 'cancelled')"
                                            class="px-4 py-2 bg-red-600 text-white rounded-lg hover:bg-red-700 font-medium text-sm transition shadow-sm">❌
                                            Cancel</button>
                                    </div>
                                    <% } else if (order.status==='confirmed' ) { %>
                                        <div class="mt-3 pt-3 border-t border-gray-200">
                                            <button onclick="updateStatus('<%=order._id %>', 'delivered')"
                                                class="w-full px-4 py-2 bg-emerald-600 text-white rounded-lg hover:bg-emerald-700 font-medium text-sm transition shadow-sm">🎉
                                                Mark Delivered</button>
                                        </div>
                                        <% } %>
                            </div>
                            <% }) %>
                    </div>
                    <% } %>
                        </div>
    </main>

    <footer class="bg-gray-800 text-white mt-12 py-6">
        <div class="max-w-full xl:max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
            <p>&copy; 2025 JeevBandhu. Building trust through transparency.</p>
        </div>
    </footer>

    <script>
        async function updateStatus(orderId, newStatus) {
            if (!confirm(`Are you sure you want to mark this order as ${newStatus}?`)) {
                return;
            }

            try {
                const response = await fetch(`/orders/${orderId}/status`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ status: newStatus })
                });

                const result = await response.json();

                if (result.success) {
                    alert(`✅ Order ${newStatus} successfully!`);
                    location.reload();
                } else {
                    alert(`❌ ${result.error}`);
                }
            } catch (error) {
                alert('❌ Failed to update order status');
            }
        }

        // Mobile menu toggle
        const salesMenuBtn = document.getElementById('salesMenuBtn');
        const salesMenu = document.getElementById('salesMenu');

        if (salesMenuBtn && salesMenu) {
            salesMenuBtn.addEventListener('click', function (e) {
                e.stopPropagation();
                salesMenu.classList.toggle('hidden');
                salesMenu.classList.toggle('active');
                salesMenuBtn.classList.toggle('active');
            });

            document.addEventListener('click', function (e) {
                if (salesMenu && !salesMenu.contains(e.target) && !salesMenuBtn.contains(e.target)) {
                    salesMenu.classList.add('hidden');
                    salesMenu.classList.remove('active');
                    salesMenuBtn.classList.remove('active');
                }
            });

            const salesLinks = salesMenu.querySelectorAll('a');
            salesLinks.forEach(link => {
                link.addEventListener('click', function () {
                    salesMenu.classList.add('hidden');
                    salesMenu.classList.remove('active');
                    salesMenuBtn.classList.remove('active');
                });
            });
        }
    </script>
</body>

</html>