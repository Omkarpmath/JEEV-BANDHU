<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Animal - JeevBandhu</title>
    <link rel="stylesheet" href="/css/output.css">
    <style>
        /* Reduce species dropdown size */
        #species {
            max-width: 300px;
        }
    </style>
</head>

<body class="bg-gray-50">
    <!-- Navigation -->
    <nav class="bg-white shadow-md">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
                <a href="/dashboard" class="text-2xl font-bold text-emerald-600">🐄 JeevBandhu</a>
                <div class="flex items-center space-x-6">
                    <a href="/dashboard" class="text-gray-700 hover:text-emerald-600 transition">Dashboard</a>
                    <span class="text-gray-600">Hi, <%= user.name %></span>
                    <form action="/logout" method="POST" class="inline">
                        <button type="submit" class="text-red-600 hover:text-red-700 transition">Logout</button>
                    </form>
                </div>
            </div>
        </div>
    </nav>

    <main class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <div class="mb-8">
            <h1 class="text-3xl font-bold text-gray-900">Add New Animal</h1>
            <p class="text-gray-600 mt-2">Register a new animal to your herd</p>
        </div>

        <form action="/animals" method="POST" class="card">
            <% if (error) { %>
                <div class="mb-4 p-4 bg-red-100 border border-red-400 text-red-700 rounded-lg">
                    <%= error %>
                </div>
                <% } %>

                    <div class="space-y-4">
                        <div>
                            <label for="tagId" class="block text-sm font-medium text-gray-700 mb-1">
                                Tag ID <span class="text-red-500">*</span>
                            </label>
                            <input type="text" id="tagId" name="tagId" required class="input-field"
                                placeholder="e.g., IND-COW-104">
                            <p class="mt-1 text-sm text-gray-500">Unique identifier for this animal</p>
                        </div>

                        <div>
                            <label for="species" class="block text-sm font-medium text-gray-700 mb-1">
                                Species <span class="text-red-500">*</span>
                            </label>
                            <select id="species" name="species" required class="input-field">
                                <option value="">Select species</option>
                                <option value="Cow">Cow</option>
                                <option value="Buffalo">Buffalo</option>
                                <option value="Goat">Goat</option>
                                <option value="Sheep">Sheep</option>
                                <option value="Poultry">Poultry</option>
                            </select>
                        </div>

                        <div>
                            <label for="breed" class="block text-sm font-medium text-gray-700 mb-1">
                                Breed
                            </label>
                            <input type="text" id="breed" name="breed" class="input-field"
                                placeholder="e.g., Gir, Holstein">
                        </div>

                        <div>
                            <label for="geneticLineage" class="block text-sm font-medium text-gray-700 mb-1">
                                Genetic Lineage
                            </label>
                            <input type="text" id="geneticLineage" name="geneticLineage" class="input-field"
                                placeholder="e.g., Sire #402">
                        </div>

                        <div>
                            <label for="dateOfBirth" class="block text-sm font-medium text-gray-700 mb-1">
                                Date of Birth
                            </label>
                            <input type="date" id="dateOfBirth" name="dateOfBirth" class="input-field">
                        </div>
                    </div>

                    <div class="mt-6 flex space-x-4">
                        <button type="submit" class="flex-1 btn-primary">
                            Add Animal
                        </button>
                        <a href="/dashboard" class="flex-1 btn-secondary text-center">
                            Cancel
                        </a>
                    </div>
        </form>
    </main>
</body>

</html>